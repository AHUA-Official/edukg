<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.mapper.GraphnodetypeMapper">

    <select id="selectAll" resultType="com.example.entity.Graphnodetype">
        select `graphnodetype`.* from `graphnodetype`

        <where>

        </where>
    </select>

    <select id="selectById" resultType="com.example.entity.Graphnodetype">
        select * from graphnodetype where id = #{id}
    </select>

    <delete id="deleteById">
        delete from graphnodetype where  id = #{id}
    </delete>

</mapper>