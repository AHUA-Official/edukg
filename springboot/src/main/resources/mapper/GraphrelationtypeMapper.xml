<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.mapper.GraphrelationtypeMapper">

    <select id="selectAll" resultType="com.example.entity.Graphrelationtype">
        select `graphrelationtype`.* from `graphrelationtype`

        <where>

        </where>
    </select>

    <select id="selectById" resultType="com.example.entity.Graphrelationtype">
        select * from graphrelationtype where id = #{id}
    </select>

    <delete id="deleteById">
        delete from graphrelationtype where  id = #{id}
    </delete>

</mapper>